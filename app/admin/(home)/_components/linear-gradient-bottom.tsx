const LinearGradientBottom = () => {
  return (
    <div
      style={{
        background: "linear-gradient(to bottom, transparent, white 70%)",
      }}
      className="absolute bottom-0 left-0 h-20 bg-white w-full z-[999]"
    />
  );
};

export default LinearGradientBottom;
